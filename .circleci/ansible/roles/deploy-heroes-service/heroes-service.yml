apiVersion: v1
kind: Service
metadata:
  name: heroes-service
  labels:
    app: heroesservice
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: heroesservice